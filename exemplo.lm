main(){
    int nota;
    nota = 69;

    if (nota >= 90) {
        imprime(1); // Conceito A
    } else {
        if (nota >= 70) {
            imprime(2); // Conceito B -> Deve executar este aqui
        } else {
            imprime(3); // Conceito C
        }
    }
}