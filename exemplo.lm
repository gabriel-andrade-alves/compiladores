int fun(int s[], float b[][]){

    impl(s[1]);
    s[1] = 8;
    impl(s[1]);
    return 1;
}

main() {
    int s[2];
    float s2[1][2];

    s[1] = 3;

    impl(s[1]);

    int a = fun(s,s2);

    impl(s[1]);
}

